// API route — stub, implemented in Stage 4
import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({ message: 'Preview manager not yet implemented' }, { status: 501 });
}
