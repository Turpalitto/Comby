import type { ModeDefinition } from "@combi/shared";

export const appMode: ModeDefinition = {
  kind: "app",
  label: "Web App",
  summary: "Interactive browser apps with state, flows, and reusable UI.",
  previewStrategy: "single-page-app"
};

