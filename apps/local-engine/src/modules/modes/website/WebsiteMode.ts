import type { ModeDefinition } from "@combi/shared";

export const websiteMode: ModeDefinition = {
  kind: "website",
  label: "Website",
  summary: "Marketing pages, landing pages, docs, and content-led sites.",
  previewStrategy: "static-site"
};

